{
  "name": "dead-drop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --test src/test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "description": "Move files between two devices with no server, no internet and no accounts."
}
